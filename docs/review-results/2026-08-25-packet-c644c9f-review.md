# Ревью пакета packet-c644c9f (круг 10) — 25.08.2026

Предмет: `efir-01072026-invoice-creation`, ветка `feature/invoice-checkbox-min`, коммит `c644c9f`
(Код.js, tests/test-door-once.js, tests/test-repeat-invoice-guard.js, tests/test-shop-clients.js).
Запуск: Фейбл (локально) и Кодекс (codex CLI, `model_reasoning_effort=xhigh`) — параллельно, на один код.

## Сводка

| | вердикт | CRIT | HIGH | MED | LOW |
|---|---|---|---|---|---|
| Фейбл | BLOCK | 1 | 0 | 3 | 3 |
| Кодекс | BLOCK | 5 | 1 | 1 | 0 |

Совпали по месту: 4 (память формы без attemptId; replay из закрытой записи без owner;
корзина резервов сверяет только с головой; игнор результата `_invNcOwnerPut_`) —
но уровни разошлись: Кодекс держит первые три как CRIT, Фейбл как MED.

---

## Фейбл — BLOCK. CRIT 1 | HIGH 0 | MED 3 | LOW 3

Прочитан весь пакет (3541 строка), гипотезы проверены по репозиторию и исполнением реального кода;
три изменённых набора тестов прогнаны — зелёные (139/329/749).

**CRIT — `Код.js:14139–14180`** (`doPost`, ветка `invstate`) + подтверждено потребителем
`warehouse/apps-script/web-app.js:8623` — четвёртый путь выдачи чужого документа: `invstate`
отвечает `phase:'done'` + номер + url по надгробию **без всякой сверки владельца**, а аппка на
`phase==='done' && attempt===мой` помечает заказ выставленным (`pinvStoreResult(..., complete:true)`,
`pinvAttemptDone_`) и показывает файл.
- Сценарий: ответ на `invoice` потерян (сеть/сон телефона — штатный случай, ради которого журнал и
  существует), кол. C сменили с Анны на Беату; аппка при открытии карточки опрашивает `invstate` →
  сервер отдаёт готовый документ Анны как терминальный итог текущего заказа Беаты → работник
  отправляет чужой инвойс. Ровно сценарий CRIT-2, закрытый в replay-путях ядра и в перелокации
  doPost, но не в этом эндпоинте: контракт восстановления у него остался слабее ядерного.
- Правка: в `invstate` перед ответом `done`/`file` с url сверять `rec.owner` (он уже пишется в
  надгробие/closed) со свежей кол. C строки заказа (`_svcFindOrderRow_` + `_readCellsDisplay_`,
  предикат `_invRowsSameOwner_`); не доказано — отдавать `owner_changed`/`row_read_failed`, как ядро.

**MED — `Код.js:7534–7546` + `8365`** — replay через закрытую рабочую запись (`_invResumePlan_`
mode `'replay'`) отдаёт прежний ответ без проверки `jrn.owner`, хотя владелец в записи есть;
несогласовано с надгробным replay, который круг 9 починил.
- Сценарий: закрытие записалось, надгробие нет; транспортный повтор согласия (repeatOk, та же метка)
  после смены кол. C: ранний надгробный чек — absent, гейт already_done выключен флагом, рубежи
  сверяют свежую кол. C саму с собой → `replay` отдаёт документ Анны как успех заказа Беаты.
  Узко (три редких условия сразу).
- Правка: в ветке `_plan.mode==='replay'` повторить ту же сверку владельца, что у надгробного replay.

**MED — `Код.js:1680–1690`** (`_markRsvAmbiguity_`, `put`) — внутри корзины новая строка сверяется
только с ПЕРВОЙ строкой корзины; чужая «голова» скрывает конфликт. Подтверждено исполнением зонда:
строки одного человека (одинаковый полный телефон, разные ники, разные полные резервы) остаются
**без метки rsvAmb**, если раньше в Базе стоит человек с совпавшей локальной частью номера из другой
страны; без него метка ставится. Итог — документ строится автоматически, пакомат выбирает сортировка
Базы. Обратная сторона: строка с номером БЕЗ кода страны транзитивно сливает двух разных людей →
ложный спор → лишняя форма.
- Правка: в корзине держать список представителей групп и сверять новую строку с каждым.

**MED — `Код.js:14259/14299/14314` + `14306`** — память владельца формы ключуется только заказом:
повторное нажатие «Инвойс» по тому же заказу перезаписывает владельца свежей кол. C.
- Сценарий: работник 1 открыл форму для Анны; кол. C сменили на Беату; работник 2 (или повторное
  нажатие) получает отказ и перештамповывает память на «беата»; работник 1 жмёт «Создать» с
  реквизитами Анны → гейт проходит → гибрид. Требует двух нажатий + правки кол. C при открытой форме.
- Правка: ключевать память ещё и attemptId (`newclient` приходит с той же меткой, что и открывший
  форму `invoice` — проверено в warehouse `web-app.js:9811`).

**LOW — `Код.js:10788`** — `_invSsOwnerState_()` (поход в Drive ~0,2 с) зовётся до ветки
`rd.state==='absent'` (самое частое состояние), где результат не используется. Перенести вызов после
раннего выхода.

**LOW — `Код.js:1652`** (`_rsvNorm_`) — не снимает диакритику: «Rīga, Brīvības 100» против
«Riga, Brivibas 100» у дублей одного человека = ложный спор → лишняя форма (fail-closed, документ не
страдает). Добавить `_stripDiacritics_`.

**LOW — `Код.js:14259/14299/14314`** — результат `_invNcOwnerPut_` игнорируется: при отказе записи
форма всё равно открывается, работник набирает всё и лишь потом получает `form_owner_unknown`.

**Ответы на вопросы пакета.** Ложных отказов новых рубежей не нашёл (штатная многострочность,
смешанный регистр, телефон/пустая кол. C, реестр — проверены: предикат `_invRowsSameOwner_` един,
все 7 вызовов ядра передают владельца, `newclient` шлёт `expectNo`). Дверь постоянно мёртвых
состояний не имеет (fail-closed только на транзиентных сбоях, Shared Drive = `none` = факт).
`_markRsvAmbiguity_` по цене — один проход, приемлемо. Мёртвой `_rsvConflict_` нет. Декоративных
гейтов в новых тестах не нашёл. Хуже относительно `b73159b` — только осознанные цены (потерянное
нажатие двери при сбое, лишний Drive-вызов).

---

## Кодекс (gpt, xhigh) — BLOCK. CRIT 5 | HIGH 1 | MED 1 | LOW 0

**CRIT — `Код.js:7027`** — память формы не разделена по `attemptId`; сценарий: форма A запомнила
`e7-101 → Anna`, строку сменили на Beata, форма B перезаписала тот же ключ, затем форма A отправила
реквизиты Анны → ядро принимает владельца Beata и создаёт гибрид «вещи Беаты + реквизиты Анны»;
правка: включить `attemptId` в ключ и требовать совпадения попытки при чтении.

**CRIT — `Код.js:7737`** — текущий `expectedOwnerIn` ошибочно считается доказательством владельца
старого документа; сценарий: надгробие предыдущей версии без `owner` относится к Анне, кол. C уже
Beata, вызывающий передаёт текущую Beata → старый инвойс Анны выдаётся как результат заказа Беаты;
правка: записи без сохранённого `owner` всегда завершать `replay_owner_unknown`.

**CRIT — `Код.js:7534`** — резервный replay из рабочей записи `phase:'closed'` вообще не проверяет
сохранённого владельца; сценарий: закрытие для Анны записалось, надгробие не записалось, при
транспортном повторе `repeatOk=true` строка уже принадлежит Беате → `_invResumePlan_` возвращает файл
Анны; правка: перед `replay` требовать `jrn.owner` и сверять со всеми свежими строками заказа.

**CRIT — `Код.js:7745`** — проверка надгробия перечитывает владельца только якорной строки; сценарий:
инвойс создан для двух строк Анны, затем во второй строке C заменена на Beata при прежнем D, повтор
приходит по первой строке → старый документ со ставшей чужой позицией выдаётся как актуальный;
правка: перед replay собрать все строки текущего номера и применить общий `_invRowsSameOwner_`.

**CRIT — `Код.js:1683`** — корзина резервов сравнивает каждую запись только с первым несовместимым
элементом; сценарий: первой стоит `+37120000015`, затем две записи одного клиента с `+37220000015`,
разными никами и разными L/M → последние две не объединяются, конфликт не ставится и один из
пакоматов молча попадает в инвойс; правка: хранить в корзине список представителей и сравнивать
новую запись со всеми несовпавшими компонентами.

**HIGH — `Код.js:657`** — существующий владелец с недоступным email ошибочно объявляется Shared
Drive; сценарий: после передачи личной таблицы `getOwner()` возвращает объект нового владельца, но
`getEmail()` — пустую строку, старый `triggerUid` совпадает с меткой → старый триггер продолжает
создавать файлы от прежнего аккаунта; правка: `none` возвращать только при `!o`, пустой email
объекта считать `unknown`.

**MED — `Код.js:14259`** — подтверждённый результат `_invNcOwnerPut_` игнорируется всеми ветками
открытия формы; сценарий: PropertiesService не сохранил запись, сервер всё равно отвечает
`no_client`, работник заполняет форму → `newclient` получает `form_owner_unknown`; правка: при
`false` сразу возвращать заметный повторяемый отказ и покрыть тестом вызывающий `doPost`.
